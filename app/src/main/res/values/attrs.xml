<resources>

    <!-- Declare custom theme attributes that allow changing which styles are
         used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is
         necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference" />
        <attr name="metaButtonBarButtonStyle" format="reference" />
    </declare-styleable>
    <declare-styleable name="NumberPickerPreference">
        <attr name="minValue" format="integer|reference" />
        <attr name="maxValue" format="integer|reference" />
        <attr name="wrapSelectorWheel" format="boolean" />
    </declare-styleable>

    <declare-styleable name="PrompterView">
        <attr name="scrollSpeed" format="integer|reference" />
        <attr name="timersCount" format="integer|reference" />
        <attr name="timerWaitingValue" format="integer|reference" />
        <attr name="timerRunningValue" format="integer|reference" />
    </declare-styleable>
</resources>
